# used to tell to Grafana where to find the JSON with the implemented configurations
apiVersion: 1
providers:
  - name: 'Retail Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/dashboards